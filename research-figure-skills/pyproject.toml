[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
[project]
name = "research-figure-skills"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["matplotlib", "numpy", "pandas", "PyYAML", "jsonschema", "Pillow", "PyMuPDF", "lxml", "networkx"]
